version: '3.4'

services:
  neuralsentinel:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8080"
    volumes:
      - ./datasets:/usr/src/app/datasets
      - ./models:/usr/src/app/models
    restart: always