version: "3.5"

services:
  neuralstrength_vis:
      build:
        context: .
        dockerfile: Dockerfile
      ports:
        - "80:80"
        - "443:443"
      volumes:
        - /home/ubuntu/hackathon/images:/images
        - /etc/letsencrypt:/etc/letsencrypt